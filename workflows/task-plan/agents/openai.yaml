interface:
  display_name: "Task Plan"
  short_description: "Plan commits with ignored TASK.md"
  default_prompt: "Use $task-plan to preserve resolved requirements in a concise behavior specification and commit plan in TASK.md before making a non-trivial code change."
