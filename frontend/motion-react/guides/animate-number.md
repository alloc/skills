# AnimateNumber

Motion+ component for animating numeric text changes. Use for counters, prices, stats, and live values where the displayed number should transition instead of jump.

Install/access Motion+ first, then keep formatting concerns explicit (locale, precision, prefixes/suffixes) in the wrapping component.
