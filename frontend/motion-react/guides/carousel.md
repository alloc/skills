# Carousel

Motion+ carousel component for animated slide navigation. Use for image/content galleries when you need Motion's drag, spring, and presence behavior bundled into a higher-level component.

Prefer wrapping it in a project-specific component that owns slide data, controls, labels, and keyboard/accessibility behavior.
